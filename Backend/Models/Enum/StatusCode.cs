namespace Backend.Models.Enum
{
    public enum StatusCode
    {
        UserNotFound = 0,
        EntityNotFound = 10,
        OK = 200,
        NotFound = 404,
        InternalServerError = 500
    }
}
